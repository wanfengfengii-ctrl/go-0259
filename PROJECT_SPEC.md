# MycoCycle 食用菌菌包转入出菇房前联检闭环

## 项目目标

MycoCycle 是面向食用菌工厂质控班组的单节点 Go HTTP 后端，用于管理一次灭菌接种后的菌包批次在转入出菇房前的联检闭环。系统在任务锁定时冻结菇种谱系、培养料配方摘要、灭菌釜次、接种线、菌包批号、抽检袋位、培养架位、探头窗口、培养日程、污染阈值、菌丝成熟度阈值、理化阈值、复核人员名单和任务代次；随后推进双人抽检确认、袋位样本封存、架位与探头占用、按培养日龄观察、杂菌培养与分子读数核验、含水率与 pH 复测、污染疑点复判、独立复核，最终只形成允许转房、已转房、污染隔离或已取消中的唯一结论。实现规模规划为 52-60 个生产 Go 文件、约 5200-6200 行有效生产 Go 代码，覆盖 catalog、inspection、occupancy、maturity、contamination、arbiter、store、api、cmd 等包；SQLite WAL 持久化目录、任务聚合、占用租约、成熟度覆盖格、理化读数、不可覆盖污染证据、仪器调用、幂等结果、审计事件和终局凭据，支持确定性重启恢复，并提供 go.mod 与 linux/amd64、linux/arm64 双架构 Docker 构建。

## 端到端业务流程

1. 创建转房联检任务：提交菇种谱系修订、培养料配方修订摘要、灭菌釜次摘要、接种线编号、菌包批号、抽检袋位集合、目标培养架位、温湿度探头窗口、培养日龄日程、污染阈值、成熟度阈值、含水率与 pH 阈值、复核人员名单；系统校验修订匹配、釜次摘要新鲜度、袋位唯一性和人员资质后进入待抽检确认。
2. 双人抽检与样本封存：两名不同合格人员分别确认抽检袋位、菌包批号和任务代次，确认完成后生成袋位样本封存记录；相同操作号同内容重试返回原结果，内容冲突返回稳定错误码。
3. 架位与探头窗口占用：任务在启动培养观察前原子占用培养架位和探头时间窗口，开放任务之间不得共享同一菌包批号、袋位、架位或探头窗口；并发锁定、换架、启动以租约账簿作确定裁定。
4. 日龄观察与成熟度覆盖：按锁定培养日程推进，每个日龄必须覆盖所有抽检袋位；观察记录包含菌丝覆盖率、污染斑计数、袋体破损标记、缺测补测原因和观察人员，非法整数读数不得写入有效覆盖格。
5. 污染与理化核验：杂菌培养、分子检测、含水率、pH、温度、湿度和二氧化碳读数按固定小数位整数规则入账；设备拒绝、断连、超时或格式错误只形成待重试调用记录，并保持占用与状态不被提前释放。
6. 复判与终局裁定：杂菌阳性、同袋位观察分歧、探头窗口断点或袋体破损会触发带任务代次的复判证据链；独立复核满足人员差异和资质后，终局仲裁器在允许转房、污染隔离、已取消之间执行单写屏障，竞争失败者得到确定拒绝。

## 核心组件与职责

1. 菇种与培养规则目录：维护虚构菇种谱系、培养料配方修订、灭菌釜次摘要、接种线能力、培养日程模板、污染阈值、成熟度阈值、理化阈值和复核人员资质；提供摘要匹配、新鲜度校验和锁定快照构造。
2. 菌包转房检验任务聚合：以菌包批号和任务代次为一致性边界，管理待锁定、待抽检确认、样本封存中、架位占用中、日龄观察中、污染核验中、理化复测中、待独立复核、可转房、已转房、污染隔离、已取消状态；集中处理幂等操作号、阶段屏障和状态迁移。
3. 袋位样本与架位探头占用账簿：维护菌包批号、抽检袋位、培养架位和探头窗口的一次性锁定与并发租约；支持原子抢占、换架、释放条件、重启恢复和按菌包批号、袋位、日龄、探头编号排序的拒绝原因。
4. 日龄成熟度及理化采集账簿：保存培养日龄覆盖矩阵、菌丝覆盖率、污染斑计数、袋体破损、缺测补测、含水率、pH、温湿度和二氧化碳读数；所有派生判断使用固定小数位整数运算并检查长度、符号、除零和溢出。
5. 污染复判及终局仲裁器：保存不可覆盖污染证据版本链、分子检测孔位、复判代次、受影响袋位与日龄范围；在污染证据闭合、成熟度达标、理化指标满足阈值和双人独立复核后生成唯一转房凭据，或生成隔离、取消结论。
6. Go HTTP API：提供 JSON 接口、稳定错误码、确定排序的错误明细、可控逻辑时钟、测试用设备脚本注入、审计查询、健康检查和重启恢复入口；无强制前端。

## 领域规则与不变量

1. 锁定快照不可变：菇种谱系、培养料配方摘要、灭菌釜次、接种线、菌包批号、袋位集合、架位、探头窗口、培养日程、阈值、复核人员名单和任务代次在锁定后只能被后续步骤引用，不能被覆盖。
2. 开放任务唯一占用：同一菌包批号、抽检袋位、培养架位或探头窗口在开放任务中最多属于一个有效任务；并发请求必须通过同一事务裁定。
3. 人员分离：抽检确认人员之间必须不同，抽检人员不得与独立复核人员角色重合，终局凭据需要两名不同且合格的复核人员。
4. 幂等一致性：每个任务代次内的操作号保存请求摘要与结果；同号同内容重试返回原结果，同号不同内容拒绝且不改变业务状态。
5. 日龄覆盖完整性：培养日程与抽检袋位形成固定矩阵，缺测只能通过同代次补测记录闭合，重复提交不能覆盖已有效写入的格。
6. 污染证据不可覆盖：杂菌阳性、观察分歧、探头窗口断点和袋体破损触发复判；旧代次迟到读数保留为审计记录但不参与当前结论。
7. 固定整数运算：含水率、pH、温度、湿度、二氧化碳、菌丝覆盖率和污染斑计数均使用明确比例尺的整数表达，解析和派生计算必须检查范围、符号、长度、除零和溢出。
8. 终局单写屏障：允许转房、已转房、污染隔离和已取消一旦写入，后续读数、复判、复核和普通操作全部拒绝且不得改变状态。

## 数据模型与持久化

1. catalog_strains：菇种谱系、谱系修订、允许培养料修订集合、默认培养日程、成熟度区间。
2. catalog_substrates：培养料配方修订、摘要、含水率目标、pH 目标、有效期与作废标记。
3. catalog_sterilizer_runs：灭菌釜次、釜次摘要、完成逻辑时间、接种线绑定、新鲜度窗口。
4. inspection_tasks：任务 ID、菌包批号、任务代次、状态、锁定快照 JSON、创建逻辑时间、终局版本。
5. locked_bag_positions：任务 ID、菌包批号、抽检袋位、封存状态、封存人员、封存逻辑时间，唯一约束覆盖开放状态。
6. occupancy_leases：资源类型、资源编号、袋位或探头编号、窗口起止、任务 ID、代次、租约状态、释放原因。
7. maturity_cells：任务 ID、代次、培养日龄、袋位、菌丝覆盖率整数、污染斑计数、袋体破损、缺测标记、观察摘要。
8. physchem_readings：任务 ID、代次、袋位、日龄、指标类型、定点整数值、比例尺、来源设备、解析状态、派生判定。
9. device_attempts：设备类型、设备编号、调用对象、任务 ID、代次、逻辑时间、脚本序号、结果类型、重试次数、错误码。
10. contamination_evidence：任务 ID、复判代次、证据版本、袋位、日龄、检测孔位、来源、阳性标记、不可覆盖摘要。
11. reviews：任务 ID、代次、复核人员、资质编号、复核结论、请求摘要、逻辑时间。
12. final_decisions：任务 ID、终局类型、凭据编号、获胜操作号、写入逻辑时间、决策摘要。

## 公开接口

1. POST /v1/tasks/lock：创建并锁定联检任务，返回任务 ID、代次、状态和锁定摘要。
2. POST /v1/tasks/{id}/sampling-confirmations：提交双人抽检确认，校验人员、袋位、任务代次和操作号。
3. POST /v1/tasks/{id}/sample-seals：提交袋位样本封存完成记录，按袋位排序返回封存结果。
4. POST /v1/tasks/{id}/occupancy/start 与 POST /v1/tasks/{id}/occupancy/move-rack：启动或变更架位及探头窗口租约，所有冲突原因确定排序。
5. POST /v1/tasks/{id}/observations：提交培养日龄观察覆盖格，支持缺测补测和重复提交校验。
6. POST /v1/tasks/{id}/device-readings：提交探头、分子检测仪、水分仪和 pH 读数，设备故障进入待重试调用。
7. POST /v1/tasks/{id}/contamination-rechecks：创建或补全污染复判证据，校验复判代次、袋位、日龄和检测孔位覆盖。
8. POST /v1/tasks/{id}/reviews 与 POST /v1/tasks/{id}/finalize：提交独立复核并竞争写入唯一终局。
9. GET /v1/tasks/{id}、GET /v1/tasks/{id}/audit、GET /healthz：查询任务快照、审计事件和服务健康状态。

## 失败边界

1. 所有写操作在 SQLite WAL 单事务内完成，失败不得留下部分样本、设备占用、观察覆盖格、派生证据或终局结论。
2. 稳定错误码覆盖修订错配、釜次摘要陈旧、袋位重复、资源窗口冲突、代次冲突、角色重合、幂等内容冲突、读数越界、算术溢出、设备故障、复判覆盖不足、终态拒绝。
3. 拒绝原因统一按菌包批号、袋位、日龄、探头编号、检测孔位排序；公开测试可精确断言错误列表。
4. 设备拒绝、断连、超时和格式错误只写入 device_attempts，保留重试对象、次数和逻辑时间，不生成合格证据。
5. 重启恢复以数据库为准重建任务状态、租约有效性、幂等结果、复判版本链和终局屏障；未闭合设备调用恢复为待重试。
6. 并发锁定、换架、启动、复核和终局竞争使用唯一索引加事务重试，竞争失败不影响获胜事务。
7. 迟到读数按原代次入审计链，当前代次状态与结论不回滚、不覆盖。

## 验收标准

1. 锁定时固定菇种谱系、培养料配方摘要、灭菌釜次、接种线、菌包批号、抽检袋位集合、培养架位、探头窗口、培养日程、污染阈值、成熟度阈值、理化阈值和任务代次。
2. 每个菌包批号、抽检袋位、培养架位和探头窗口在开放任务中只能被一个有效任务占用，并发锁定、换架或启动必须原子裁定。
3. 抽检确认、样本封存、观察提交与复核必须匹配当前任务代次；相同操作号同内容重试幂等，内容冲突报错，陈旧配方或错误釜次不得推进状态。
4. 日龄观察必须覆盖锁定日程和抽检袋位矩阵，菌丝覆盖率、污染斑数量、袋体破损标记和缺测补测均使用确定整数规则，非法读数不得写入有效覆盖格。
5. 含水率、pH、培养温度、湿度和二氧化碳读数全部使用固定小数位整数运算并检查长度、符号、除零和溢出，算术失败不得写入派生证据。
6. 培养箱探头、分子检测仪或水分仪拒绝、断连、超时、格式错误只能形成可审计待重试调用，重试对象、次数和逻辑时间可确定，不得伪造合格结果或提前释放占用。
7. 出现杂菌阳性、同袋位观察分歧、温湿度窗口断点或袋体破损时只能创建一个带代次的复判证据，复判须覆盖受影响袋位、日龄和检测孔位，旧代次迟到读数保持不可覆盖且不影响当前结论。
8. 全部日龄覆盖格完整、成熟度达标、污染证据闭合、理化指标满足锁定阈值，并由两名不同且合格人员独立复核后，竞争成功者才可生成唯一转房凭据；并发转房、污染隔离与取消只能形成一个终局，终态后的读数、复判、复核或普通操作一律被拒绝且不改变状态。

## 确定性测试场景

1. lock_test.go：锁定成功冻结完整快照；培养料摘要错配拒绝；灭菌釜次摘要陈旧拒绝；抽检袋位重复返回确定排序错误。
2. occupancy_test.go：两个任务并发抢同一菌包批号和袋位只成功一个；架位窗口重叠被拒绝；探头窗口边界相邻可通过；换架与启动同步竞争保持原子性。
3. idempotency_test.go：同操作号同内容重试返回同一结果；同操作号不同内容拒绝；旧任务代次提交抽检确认、样本封存、观察和复核均拒绝。
4. maturity_test.go：日龄与袋位矩阵完整后进入污染核验；缺失日龄不得推进；重复观察不覆盖有效格；菌丝覆盖率阈值边界和污染斑计数边界可确定断言。
5. physchem_test.go：含水率、pH、温度、湿度、二氧化碳定点整数解析成功；超长、负号非法、比例尺不符、除零和溢出全部不写入派生证据。
6. device_retry_test.go：可控探头断连、分子检测仪超时、水分仪格式错误写入待重试调用；重试次数和逻辑时间确定；故障期间租约仍保持。
7. contamination_test.go：杂菌阳性创建单一复判证据；同袋位观察分歧需要覆盖对应日龄；旧复判代次迟到读数只进审计；复判代次冲突拒绝。
8. finality_test.go：两名合格独立复核后并发转房只生成一个凭据；污染隔离与取消竞争只保留一个终局；终态后的读数、复判和复核被稳定拒绝；重启后终局屏障仍有效。

## 组件追踪关系

1. 菇种与培养规则目录 -> 验收 1、3；覆盖修订匹配、釜次新鲜度、培养日程和阈值快照。
2. 菌包转房检验任务聚合 -> 验收 1、3、8；覆盖状态机、任务代次、幂等操作号和终局状态。
3. 袋位样本与架位探头占用账簿 -> 验收 2、6；覆盖袋位样本封存、架位租约、探头窗口租约和设备故障期间的占用保持。
4. 日龄成熟度及理化采集账簿 -> 验收 4、5；覆盖培养日龄矩阵、整数观察规则、固定小数位理化读数和算术错误隔离。
5. 污染复判及终局仲裁器 -> 验收 7、8；覆盖污染证据版本链、复判代次隔离、独立复核和唯一结论。
6. Go HTTP API -> 验收 2、3、6、8；覆盖并发请求、稳定错误码、确定排序原因、逻辑时钟、设备脚本和重启恢复测试入口。

## 独特性

该项目的核心业务锚点是食用菌工厂中菌包从培养阶段进入出菇房前的质量闭环，关键状态来自菌包批号、抽检袋位、培养架位、培养日龄、探头窗口、菌丝成熟度、杂菌复判和理化复测之间的耦合。实现规模由菌包袋位矩阵、架位与探头租约、固定整数读数、不可覆盖污染证据链、复核人员分离和终局单写屏障自然产生，保持在一个连贯的农业食品质控流程内。
